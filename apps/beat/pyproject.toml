[project]
name = "cm-beat"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = [
  "cm-shared",
  "celery[redis]>=5.5.3",
]

[tool.uv]
package = true

[tool.uv.sources]
cm-shared = { workspace = true }
